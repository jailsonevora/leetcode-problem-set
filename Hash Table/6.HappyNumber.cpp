#include<iostream>
#include<unordered_set>
#include<vector>

using namespace std;

class Solution {
public:
    bool isHappy(int n) {
        
    }
};

int main(){

    ios::sync_with_stdio(false);
    cin.tie();
    cout.tie();

    int n;
    cin >> n;

    Solution s;
    s.isHappy(n); 
}